{
  "routes": [
    { "handle": "content" },
    { "src": "/api/.*", "dest": "server.js" }
  ],
  "rewrites": [{ "source": "/:any*", "destination": "/" }],
  "builds": [{ "src": "server.js", "use": "@vercel/node" }]
}

