{
  "builds": [
    { "src": "index.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "index.js" },
    { "src": "/(.*)", "dest": "public/$1" }
  ],
  "build": {
    "src": "index.js",
    "output": "build",
    "include": [
      "routes/**"
    ]
  }
}
